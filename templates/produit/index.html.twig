{% extends 'base.html.twig' %}

{% block title %}Shop{% endblock %}

{% block body %}
<div class="shop">
<h1>Produits</h1>

<div class="prodAll">
    {% for produit in pagination %}
        <div class="cardProd">
            <a href="{{path('app_detail_produit', {'id': produit.id})}}"><img src="assets/img/{{produit.image}}" alt="img du produit"></a>
            <p>{{produit}} {{produit.prix}}€</p>
            <a href="{{path('app_panier_add', {'id': produit.id})}}" class="buttonSmall">Ajouter au panier</a>
        </div>
    {% endfor %}
</div>
    {{ knp_pagination_render(pagination)}}
</div>
{% endblock %}
